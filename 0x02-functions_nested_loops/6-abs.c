#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * _abs - return positive data
 *
 * @n: number is returned
 * Return: n
 */
int _abs(int n)
{
if (n < 0)
{
return (-n);
}
else
{
return (n);
}
}
