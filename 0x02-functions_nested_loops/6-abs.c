#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * _abc - return positive data
 *
 * @n: number is returned
 * Return: n
 */
int _abc(int n)
{
if (n < 0)
{
return (-n);
}
else
{
return (n);
}
}
